# Odd or Even Function Factory

This is a JavaScript program where a function returns another function based on user request.

## What it does

* If request is "odd", it returns a function to check odd numbers
* If request is "even", it returns a function to check even numbers
* Then the returned function is used

## What I learned

* Function returning another function
* Closures (basic idea)
* Conditions in JavaScript

---
